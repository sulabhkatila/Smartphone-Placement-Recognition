name: Build iOS App

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build iOS App
        run: |
          xcodebuild \
            -project PhonePlacementApp/PhonePlacementApp.xcodeproj \
            -scheme PhonePlacementApp \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' \
            clean build

  test-server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        working-directory: ./server
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt
      - name: Build and Test Server
        working-directory: ./server
        run: |
          go build -o wheremyphoneat
          ./wheremyphoneat &
          PID=$!
          sleep 5
          curl --fail http://127.0.0.1:8000/ || exit 1
          kill $PID
